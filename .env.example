# =========================
# WHISPER CONFIGURATION
# =========================
# Configuración optimizada para GPU NVIDIA RTX 3060
WHISPER_MODEL_NAME=medium
WHISPER_DEVICE=cuda

# =========================
# GEMINI MODEL CONFIGURATION (Optimized for Quality + Cost)
# =========================
# Google Gemini API Key (Get from: https://aistudio.google.com/apikey)
GOOGLE_API_KEY=

# Premium model for script synthesis and generation (CRITICAL tasks)
# Cost: $2.50 input / $15.00 output per 1M tokens
GEMINI_PRO_MODEL=gemini-2.5-pro

# Balanced model for video summarization (free tier)
# Cost: FREE in AI Studio (with rate limits)
SUMMARIZER_MODEL=gemini-2.5-flash

# Model for pattern analysis from transcripts (free tier)
# Cost: FREE in AI Studio (with rate limits)
PATTERN_ANALYZER_MODEL=gemini-2.5-flash

# Lite model for simple translations (summaries)
# Cost: $0.075 input / $0.30 output per 1M tokens
TRANSLATOR_MODEL=gemini-2.5-flash-lite

# Lite model for query optimization
# Cost: $0.075 input / $0.30 output per 1M tokens
QUERY_OPTIMIZER_MODEL=gemini-2.5-flash-lite

# =========================
# DIRECTORY CONFIGURATION
# =========================
LOG_LEVEL=INFO
TEMP_DOWNLOAD_DIR=temp_files/
OUTPUT_TRANSCRIPTS_DIR=output_transcripts/
TEMP_BATCH_DIR=temp_batch/
OUTPUT_SCRIPTS_DIR=output_scripts/
OUTPUT_ANALYSIS_DIR=output_analysis/
